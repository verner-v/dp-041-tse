$("document").ready(function() 
{
	
	/*
	$('.navbar li').click(function(e) {
		$('.navbar li').removeClass('active');
		var $this = $(this);
		if (!$this.hasClass('active')) {
		$this.addClass('active');
		}
		e.preventDefault();
		});
	*/
	
	/*
	$('.menu').children('li').each(function(index, element) {
        var url = window.location;
        if($(element).find('a').attr('href') == url.pathname)
        {
        	$(element).find('a').find('div').each(function(index,el) {
        			if($(el).attr('qqq'))
            			$(el).addClass('active');        			
        		}
        	)
       	}
    }
	);*/
});


/*	
	//$("#time1").timePicker();
	
	$('#timepicker_vic').timepicker();
	
	
	$('.timepicker-default').timepicker({
		"showMeridian":false,
		"minuteStep":1
	});
	
	//$('#dpl').datepicker();
	
	//$('#dpr').datepicker();
	
	$('#datepicker_vic').datepicker();
	
	var currentDate = new Date();  
	$("#dpl").datepicker("setValue",currentDate);
	$("#dpr").datepicker("setValue",currentDate);
	$('#datepicker_vic').datepicker("setValue",currentDate);
	
	}

);	
	*/

	
	/*
			$.datepicker.setDefaults( $.datepicker.regional[ "ru" ] );
			$( "#datepicker" ).datepicker(
				{ 
					showOn: "button", 
					buttonImage: "/assets/images/calendar.gif", 
					buttonImageOnly: true
				}
			);
			$( "#datepicker" ).datepicker( $.datepicker.regional[ "ru" ] );
			$( "#datepicker" ).datepicker( "option", "dateFormat","dd-mm-yy" );
			
			
			$( "#datepicker2" ).datepicker(
				{ 
					showOn: "button", 
					buttonImage: "/assets/images/calendar.gif", 
					buttonImageOnly: true,
				}
			);
			$( "#datepicker2" ).datepicker( $.datepicker.regional[ "ru" ] );
			$( "#datepicker2" ).datepicker( "option", "dateFormat","dd-mm-yy" );
			
			var currentDate = new Date();  
			$("#datepicker").datepicker("setDate",currentDate);
			$("#datepicker2").datepicker("setDate",currentDate);
			*/
	
//alert('hello kitty');
		
	
